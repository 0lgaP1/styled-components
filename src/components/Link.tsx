import styled from "styled-components";

export const Link = styled.a`
    background-color: transparent;
    border-radius: 10px;
    color: #131111;
    padding: 0;

    ${Link} {
        cursor: zoom-in;
    }
`